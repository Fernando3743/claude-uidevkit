// Dev-only capture sink for the claude-uidevkit overlay.
// Generated by `npx claude-uidevkit init`. Edit freely.
//
// The handler 404s in production and only accepts local/ngrok hosts, so it adds no
// production attack surface. To allow other dev-tunnel hosts (e.g. Cloudflare) or
// change the queue dir, swap this for:
//
//   import { createUIDevkitRoute } from "claude-uidevkit/server";
//   export const { POST } = createUIDevkitRoute({ extraHosts: [/\.trycloudflare\.com$/] });
export { POST } from "claude-uidevkit/server";
